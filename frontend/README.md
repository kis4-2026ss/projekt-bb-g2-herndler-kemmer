# User interface for the RAG Backend

## Setup

Startup the nginx webserver via `docker compose up -d` and open [localhost:8643](http://localhost:8643)

## Usage

This page has a simple textbox where ones promp can be inserted, currently the database that can be prompted is hardcoded.

If the request was successfull it will be returend down below the submit button
